const arr = new Array(10);
const array = [];

// Se pueden agregar cualquier tipo de cosas en los arreglos.
let videoGames = ["Fifa25", "Spider Man", true, 2];

let lastOne = videoGames[videoGames.length - 1];


// - push: Añadir un elemento al final
// - unshift: Añadir un elemento al inicio
// - pop(): Remueve el último elemento de un arreglo y lo retorna
// - splice(): Remueve elementos de un arreglo en una posición especifica y número de elementos a borrar
// - indexOf(): Obtiene el indice de un elemento de un arreglo




// Palabras reservadas:

// break, case, catch, class, const, continue, debugger, default, delete, do, else, export, extends, finally, for, function, if,
// import, in, instanceof, let, new, return, super, switch, this, throw, try, typeof, var, void, while, with, yield.

// --------------------------

// Palabras reservadas en un futuro:

// enum, implements, interface, package, private, protected, public, static, await.

// --------------------------

// Evitar usar:

// null, false, isNaN, NaN, isPrototypeOf, name, String, alert, undefined, hasOwnProperty, Infinity, length, prototype, Number,
// toString, true, undefined, isFinite, Math, valueOf, Object, prompt, conform.
